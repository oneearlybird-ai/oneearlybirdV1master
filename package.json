{
  "name": "earlybird-starter",
  "private": true,
  "packageManager": "pnpm@9",
  "scripts": {
    "dev": "concurrently \"pnpm -C apps/api dev\" \"pnpm -C apps/web dev\"",
    "build": "pnpm -C apps/api build && pnpm -C apps/web build",
    "lint": "echo 'lint ok'"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}